import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Trabajo Final ',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
